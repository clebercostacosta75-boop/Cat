{
  "name": "Instructor",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome do instrutor"
    },
    "hourly_rate": {
      "type": "number",
      "description": "Valor por hora (Custo HP)"
    },
    "specialty": {
      "type": "string",
      "description": "Especialidade do instrutor"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email do instrutor"
    },
    "phone": {
      "type": "string",
      "description": "Telefone de contato"
    }
  },
  "required": [
    "name"
  ]
}